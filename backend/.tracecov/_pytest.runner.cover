       # mypy: allow-untyped-defs
>>>>>> """Basic collect and runtest protocol implementations."""
       
>>>>>> from __future__ import annotations
       
>>>>>> import bdb
>>>>>> from collections.abc import Callable
>>>>>> import dataclasses
>>>>>> import os
>>>>>> import sys
>>>>>> import types
>>>>>> from typing import cast
>>>>>> from typing import final
>>>>>> from typing import Generic
>>>>>> from typing import Literal
>>>>>> from typing import TYPE_CHECKING
>>>>>> from typing import TypeVar
       
>>>>>> from .config import Config
>>>>>> from .reports import BaseReport
>>>>>> from .reports import CollectErrorRepr
>>>>>> from .reports import CollectReport
>>>>>> from .reports import TestReport
>>>>>> from _pytest import timing
>>>>>> from _pytest._code.code import ExceptionChainRepr
>>>>>> from _pytest._code.code import ExceptionInfo
>>>>>> from _pytest._code.code import TerminalRepr
>>>>>> from _pytest.config.argparsing import Parser
>>>>>> from _pytest.deprecated import check_ispytest
>>>>>> from _pytest.nodes import Collector
>>>>>> from _pytest.nodes import Directory
>>>>>> from _pytest.nodes import Item
>>>>>> from _pytest.nodes import Node
>>>>>> from _pytest.outcomes import Exit
>>>>>> from _pytest.outcomes import OutcomeException
>>>>>> from _pytest.outcomes import Skipped
>>>>>> from _pytest.outcomes import TEST_OUTCOME
       
       
>>>>>> if sys.version_info < (3, 11):
>>>>>>     from exceptiongroup import BaseExceptionGroup
       
>>>>>> if TYPE_CHECKING:
>>>>>>     from _pytest.main import Session
>>>>>>     from _pytest.terminal import TerminalReporter
       
       #
       # pytest plugin hooks.
       
       
>>>>>> def pytest_addoption(parser: Parser) -> None:
    1:     group = parser.getgroup("terminal reporting", "Reporting", after="general")
    2:     group.addoption(
    1:         "--durations",
    1:         action="store",
    1:         type=int,
    1:         default=None,
    1:         metavar="N",
    1:         help="Show N slowest setup/test durations (N=0 for all)",
           )
    2:     group.addoption(
    1:         "--durations-min",
    1:         action="store",
    1:         type=float,
    1:         default=None,
    1:         metavar="N",
    1:         help="Minimal duration in seconds for inclusion in slowest list. "
               "Default: 0.005 (or 0.0 if -vv is given).",
           )
       
       
>>>>>> def pytest_terminal_summary(terminalreporter: TerminalReporter) -> None:
    1:     durations = terminalreporter.config.option.durations
    1:     durations_min = terminalreporter.config.option.durations_min
    1:     verbose = terminalreporter.config.get_verbosity()
    1:     if durations is None:
    1:         return
>>>>>>     if durations_min is None:
>>>>>>         durations_min = 0.005 if verbose < 2 else 0.0
>>>>>>     tr = terminalreporter
>>>>>>     dlist = []
>>>>>>     for replist in tr.stats.values():
>>>>>>         for rep in replist:
>>>>>>             if hasattr(rep, "duration"):
>>>>>>                 dlist.append(rep)
>>>>>>     if not dlist:
>>>>>>         return
>>>>>>     dlist.sort(key=lambda x: x.duration, reverse=True)
>>>>>>     if not durations:
>>>>>>         tr.write_sep("=", "slowest durations")
           else:
>>>>>>         tr.write_sep("=", f"slowest {durations} durations")
>>>>>>         dlist = dlist[:durations]
       
>>>>>>     for i, rep in enumerate(dlist):
>>>>>>         if rep.duration < durations_min:
>>>>>>             tr.write_line("")
>>>>>>             message = f"({len(dlist) - i} durations < {durations_min:g}s hidden."
>>>>>>             if terminalreporter.config.option.durations_min is None:
>>>>>>                 message += "  Use -vv to show these durations."
>>>>>>             message += ")"
>>>>>>             tr.write_line(message)
>>>>>>             break
>>>>>>         tr.write_line(f"{rep.duration:02.2f}s {rep.when:<8} {rep.nodeid}")
       
       
>>>>>> def pytest_sessionstart(session: Session) -> None:
    1:     session._setupstate = SetupState()
       
       
>>>>>> def pytest_sessionfinish(session: Session) -> None:
    1:     session._setupstate.teardown_exact(None)
       
       
>>>>>> def pytest_runtest_protocol(item: Item, nextitem: Item | None) -> bool:
   89:     ihook = item.ihook
   89:     ihook.pytest_runtest_logstart(nodeid=item.nodeid, location=item.location)
   89:     runtestprotocol(item, nextitem=nextitem)
   89:     ihook.pytest_runtest_logfinish(nodeid=item.nodeid, location=item.location)
   89:     return True
       
       
>>>>>> def runtestprotocol(
>>>>>>     item: Item, log: bool = True, nextitem: Item | None = None
>>>>>> ) -> list[TestReport]:
   89:     hasrequest = hasattr(item, "_request")
   89:     if hasrequest and not item._request:  # type: ignore[attr-defined]
               # This only happens if the item is re-run, as is done by
               # pytest-rerunfailures.
>>>>>>         item._initrequest()  # type: ignore[attr-defined]
   89:     rep = call_and_report(item, "setup", log)
   89:     reports = [rep]
   89:     if rep.passed:
   89:         if item.config.getoption("setupshow", False):
>>>>>>             show_test_item(item)
   89:         if not item.config.getoption("setuponly", False):
   89:             reports.append(call_and_report(item, "call", log))
           # If the session is about to fail or stop, teardown everything - this is
           # necessary to correctly report fixture teardown errors (see #11706)
   89:     if item.session.shouldfail or item.session.shouldstop:
>>>>>>         nextitem = None
   89:     reports.append(call_and_report(item, "teardown", log, nextitem=nextitem))
           # After all teardown hooks have been called
           # want funcargs and request info to go away.
   89:     if hasrequest:
   89:         item._request = False  # type: ignore[attr-defined]
   89:         item.funcargs = None  # type: ignore[attr-defined]
   89:     return reports
       
       
>>>>>> def show_test_item(item: Item) -> None:
           """Show test function, parameters and the fixtures of the test item."""
>>>>>>     tw = item.config.get_terminal_writer()
>>>>>>     tw.line()
>>>>>>     tw.write(" " * 8)
>>>>>>     tw.write(item.nodeid)
>>>>>>     used_fixtures = sorted(getattr(item, "fixturenames", []))
>>>>>>     if used_fixtures:
>>>>>>         tw.write(" (fixtures used: {})".format(", ".join(used_fixtures)))
>>>>>>     tw.flush()
       
       
>>>>>> def pytest_runtest_setup(item: Item) -> None:
   89:     _update_current_test_var(item, "setup")
   89:     item.session._setupstate.setup(item)
       
       
>>>>>> def pytest_runtest_call(item: Item) -> None:
   89:     _update_current_test_var(item, "call")
   89:     try:
   89:         del sys.last_type
>>>>>>         del sys.last_value
>>>>>>         del sys.last_traceback
>>>>>>         if sys.version_info >= (3, 12, 0):
>>>>>>             del sys.last_exc  # type:ignore[attr-defined]
   89:     except AttributeError:
   89:         pass
   89:     try:
   89:         item.runtest()
>>>>>>     except Exception as e:
               # Store trace info to allow postmortem debugging
>>>>>>         sys.last_type = type(e)
>>>>>>         sys.last_value = e
>>>>>>         if sys.version_info >= (3, 12, 0):
>>>>>>             sys.last_exc = e  # type:ignore[attr-defined]
>>>>>>         assert e.__traceback__ is not None
               # Skip *this* frame
>>>>>>         sys.last_traceback = e.__traceback__.tb_next
>>>>>>         raise
       
       
>>>>>> def pytest_runtest_teardown(item: Item, nextitem: Item | None) -> None:
   89:     _update_current_test_var(item, "teardown")
   89:     item.session._setupstate.teardown_exact(nextitem)
   89:     _update_current_test_var(item, None)
       
       
>>>>>> def _update_current_test_var(
>>>>>>     item: Item, when: Literal["setup", "call", "teardown"] | None
>>>>>> ) -> None:
           """Update :envvar:`PYTEST_CURRENT_TEST` to reflect the current item and stage.
       
           If ``when`` is None, delete ``PYTEST_CURRENT_TEST`` from the environment.
           """
  356:     var_name = "PYTEST_CURRENT_TEST"
  356:     if when:
  267:         value = f"{item.nodeid} ({when})"
               # don't allow null bytes on environment variables (see #2644, #2957)
  267:         value = value.replace("\x00", "(null)")
  267:         os.environ[var_name] = value
           else:
   89:         os.environ.pop(var_name)
       
       
>>>>>> def pytest_report_teststatus(report: BaseReport) -> tuple[str, str, str] | None:
  267:     if report.when in ("setup", "teardown"):
  178:         if report.failed:
                   #      category, shortletter, verbose-word
>>>>>>             return "error", "E", "ERROR"
  178:         elif report.skipped:
>>>>>>             return "skipped", "s", "SKIPPED"
               else:
  178:             return "", "", ""
   89:     return None
       
       
       #
       # Implementation
       
       
>>>>>> def call_and_report(
>>>>>>     item: Item, when: Literal["setup", "call", "teardown"], log: bool = True, **kwds
>>>>>> ) -> TestReport:
  267:     ihook = item.ihook
  267:     if when == "setup":
   89:         runtest_hook: Callable[..., None] = ihook.pytest_runtest_setup
  178:     elif when == "call":
   89:         runtest_hook = ihook.pytest_runtest_call
   89:     elif when == "teardown":
   89:         runtest_hook = ihook.pytest_runtest_teardown
           else:
>>>>>>         assert False, f"Unhandled runtest hook case: {when}"
       
  534:     call = CallInfo.from_call(
  534:         lambda: runtest_hook(item=item, **kwds),
  267:         when=when,
  267:         reraise=get_reraise_exceptions(item.config),
           )
  267:     report: TestReport = ihook.pytest_runtest_makereport(item=item, call=call)
  267:     if log:
  267:         ihook.pytest_runtest_logreport(report=report)
  267:     if check_interactive_exception(call, report):
>>>>>>         ihook.pytest_exception_interact(node=item, call=call, report=report)
  267:     return report
       
       
>>>>>> def get_reraise_exceptions(config: Config) -> tuple[type[BaseException], ...]:
           """Return exception types that should not be suppressed in general."""
  267:     reraise: tuple[type[BaseException], ...] = (Exit,)
  267:     if not config.getoption("usepdb", False):
  267:         reraise += (KeyboardInterrupt,)
  267:     return reraise
       
       
>>>>>> def check_interactive_exception(call: CallInfo[object], report: BaseReport) -> bool:
           """Check whether the call raised an exception that should be reported as
           interactive."""
  303:     if call.excinfo is None:
               # Didn't raise.
  303:         return False
>>>>>>     if hasattr(report, "wasxfail"):
               # Exception was expected.
>>>>>>         return False
>>>>>>     if isinstance(call.excinfo.value, Skipped | bdb.BdbQuit):
               # Special control flow exception.
>>>>>>         return False
>>>>>>     return True
       
       
>>>>>> TResult = TypeVar("TResult", covariant=True)
       
       
>>>>>> @final
>>>>>> @dataclasses.dataclass
>>>>>> class CallInfo(Generic[TResult]):
           """Result/Exception info of a function invocation."""
       
>>>>>>     _result: TResult | None
           #: The captured exception of the call, if it raised.
>>>>>>     excinfo: ExceptionInfo[BaseException] | None
           #: The system time when the call started, in seconds since the epoch.
>>>>>>     start: float
           #: The system time when the call ended, in seconds since the epoch.
>>>>>>     stop: float
           #: The call duration, in seconds.
>>>>>>     duration: float
           #: The context of invocation: "collect", "setup", "call" or "teardown".
>>>>>>     when: Literal["collect", "setup", "call", "teardown"]
       
>>>>>>     def __init__(
               self,
>>>>>>         result: TResult | None,
>>>>>>         excinfo: ExceptionInfo[BaseException] | None,
>>>>>>         start: float,
>>>>>>         stop: float,
>>>>>>         duration: float,
>>>>>>         when: Literal["collect", "setup", "call", "teardown"],
               *,
>>>>>>         _ispytest: bool = False,
>>>>>>     ) -> None:
  303:         check_ispytest(_ispytest)
  303:         self._result = result
  303:         self.excinfo = excinfo
  303:         self.start = start
  303:         self.stop = stop
  303:         self.duration = duration
  303:         self.when = when
       
>>>>>>     @property
>>>>>>     def result(self) -> TResult:
               """The return value of the call, if it didn't raise.
       
               Can only be accessed if excinfo is None.
               """
   36:         if self.excinfo is not None:
>>>>>>             raise AttributeError(f"{self!r} has no valid result")
               # The cast is safe because an exception wasn't raised, hence
               # _result has the expected function return type (which may be
               #  None, that's why a cast and not an assert).
   36:         return cast(TResult, self._result)
       
>>>>>>     @classmethod
>>>>>>     def from_call(
               cls,
>>>>>>         func: Callable[[], TResult],
>>>>>>         when: Literal["collect", "setup", "call", "teardown"],
>>>>>>         reraise: type[BaseException] | tuple[type[BaseException], ...] | None = None,
>>>>>>     ) -> CallInfo[TResult]:
               """Call func, wrapping the result in a CallInfo.
       
               :param func:
                   The function to call. Called without arguments.
               :type func: Callable[[], _pytest.runner.TResult]
               :param when:
                   The phase in which the function is called.
               :param reraise:
                   Exception or exceptions that shall propagate if raised by the
                   function, instead of being wrapped in the CallInfo.
               """
  303:         excinfo = None
  303:         instant = timing.Instant()
  303:         try:
  303:             result: TResult | None = func()
>>>>>>         except BaseException:
>>>>>>             excinfo = ExceptionInfo.from_current()
>>>>>>             if reraise is not None and isinstance(excinfo.value, reraise):
>>>>>>                 raise
>>>>>>             result = None
  303:         duration = instant.elapsed()
  606:         return cls(
  303:             start=duration.start.time,
  303:             stop=duration.stop.time,
  303:             duration=duration.seconds,
  303:             when=when,
  303:             result=result,
  303:             excinfo=excinfo,
  303:             _ispytest=True,
               )
       
>>>>>>     def __repr__(self) -> str:
>>>>>>         if self.excinfo is None:
>>>>>>             return f"<CallInfo when={self.when!r} result: {self._result!r}>"
>>>>>>         return f"<CallInfo when={self.when!r} excinfo={self.excinfo!r}>"
       
       
>>>>>> def pytest_runtest_makereport(item: Item, call: CallInfo[None]) -> TestReport:
  267:     return TestReport.from_item_and_call(item, call)
       
       
>>>>>> def pytest_make_collect_report(collector: Collector) -> CollectReport:
   36:     def collect() -> list[Item | Collector]:
               # Before collecting, if this is a Directory, load the conftests.
               # If a conftest import fails to load, it is considered a collection
               # error of the Directory collector. This is why it's done inside of the
               # CallInfo wrapper.
               #
               # Note: initial conftests are loaded early, not here.
   36:         if isinstance(collector, Directory):
    4:             collector.config.pluginmanager._loadconftestmodules(
    2:                 collector.path,
    2:                 collector.config.getoption("importmode"),
    2:                 rootpath=collector.config.rootpath,
    4:                 consider_namespace_packages=collector.config.getini(
    2:                     "consider_namespace_packages"
                       ),
                   )
       
   36:         return list(collector.collect())
       
   72:     call = CallInfo.from_call(
   36:         collect, "collect", reraise=(KeyboardInterrupt, SystemExit)
           )
   36:     longrepr: None | tuple[str, int, str] | str | TerminalRepr = None
   36:     if not call.excinfo:
   36:         outcome: Literal["passed", "skipped", "failed"] = "passed"
           else:
>>>>>>         skip_exceptions = [Skipped]
>>>>>>         unittest = sys.modules.get("unittest")
>>>>>>         if unittest is not None:
>>>>>>             skip_exceptions.append(unittest.SkipTest)
>>>>>>         if isinstance(call.excinfo.value, tuple(skip_exceptions)):
>>>>>>             outcome = "skipped"
>>>>>>             r_ = collector._repr_failure_py(call.excinfo, "line")
>>>>>>             assert isinstance(r_, ExceptionChainRepr), repr(r_)
>>>>>>             r = r_.reprcrash
>>>>>>             assert r
>>>>>>             longrepr = (str(r.path), r.lineno, r.message)
               else:
>>>>>>             outcome = "failed"
>>>>>>             errorinfo = collector.repr_failure(call.excinfo)
>>>>>>             if not hasattr(errorinfo, "toterminal"):
>>>>>>                 assert isinstance(errorinfo, str)
>>>>>>                 errorinfo = CollectErrorRepr(errorinfo)
>>>>>>             longrepr = errorinfo
   36:     result = call.result if not call.excinfo else None
   36:     rep = CollectReport(collector.nodeid, outcome, longrepr, result)
   36:     rep.call = call  # type: ignore # see collect_one_node
   36:     return rep
       
       
>>>>>> class SetupState:
           """Shared state for setting up/tearing down test items or collectors
           in a session.
       
           Suppose we have a collection tree as follows:
       
           <Session session>
               <Module mod1>
                   <Function item1>
               <Module mod2>
                   <Function item2>
       
           The SetupState maintains a stack. The stack starts out empty:
       
               []
       
           During the setup phase of item1, setup(item1) is called. What it does
           is:
       
               push session to stack, run session.setup()
               push mod1 to stack, run mod1.setup()
               push item1 to stack, run item1.setup()
       
           The stack is:
       
               [session, mod1, item1]
       
           While the stack is in this shape, it is allowed to add finalizers to
           each of session, mod1, item1 using addfinalizer().
       
           During the teardown phase of item1, teardown_exact(item2) is called,
           where item2 is the next item to item1. What it does is:
       
               pop item1 from stack, run its teardowns
               pop mod1 from stack, run its teardowns
       
           mod1 was popped because it ended its purpose with item1. The stack is:
       
               [session]
       
           During the setup phase of item2, setup(item2) is called. What it does
           is:
       
               push mod2 to stack, run mod2.setup()
               push item2 to stack, run item2.setup()
       
           Stack:
       
               [session, mod2, item2]
       
           During the teardown phase of item2, teardown_exact(None) is called,
           because item2 is the last item. What it does is:
       
               pop item2 from stack, run its teardowns
               pop mod2 from stack, run its teardowns
               pop session from stack, run its teardowns
       
           Stack:
       
               []
       
           The end!
           """
       
>>>>>>     def __init__(self) -> None:
               # The stack is in the dict insertion order.
    1:         self.stack: dict[
                   Node,
                   tuple[
                       # Node's finalizers.
                       list[Callable[[], object]],
                       # Node's exception and original traceback, if its setup raised.
                       tuple[OutcomeException | Exception, types.TracebackType | None] | None,
                   ],
    1:         ] = {}
       
>>>>>>     def setup(self, item: Item) -> None:
               """Setup objects along the collector chain to the item."""
   89:         needed_collectors = item.listchain()
       
               # If a collector fails its setup, fail its entire subtree of items.
               # The setup is not retried for each item - the same exception is used.
  499:         for col, (finalizers, exc) in self.stack.items():
  410:             assert col in needed_collectors, "previous item was not torn down properly"
  410:             if exc:
>>>>>>                 raise exc[0].with_traceback(exc[1])
       
  213:         for col in needed_collectors[len(self.stack) :]:
  124:             assert col not in self.stack
                   # Push onto the stack.
  124:             self.stack[col] = ([col.teardown], None)
  124:             try:
  124:                 col.setup()
>>>>>>             except TEST_OUTCOME as exc:
>>>>>>                 self.stack[col] = (self.stack[col][0], (exc, exc.__traceback__))
>>>>>>                 raise
       
>>>>>>     def addfinalizer(self, finalizer: Callable[[], object], node: Node) -> None:
               """Attach a finalizer to the given node.
       
               The node must be currently active in the stack.
               """
   61:         assert node and not isinstance(node, tuple)
   61:         assert callable(finalizer)
   61:         assert node in self.stack, (node, self.stack)
   61:         self.stack[node][0].append(finalizer)
       
>>>>>>     def teardown_exact(self, nextitem: Item | None) -> None:
               """Teardown the current stack up until reaching nodes that nextitem
               also descends from.
       
               When nextitem is None (meaning we're at the last item), the entire
               stack is torn down.
               """
   90:         needed_collectors = (nextitem and nextitem.listchain()) or []
   90:         exceptions: list[BaseException] = []
  214:         while self.stack:
  212:             if list(self.stack.keys()) == needed_collectors[: len(self.stack)]:
   88:                 break
  124:             node, (finalizers, _) = self.stack.popitem()
  124:             these_exceptions = []
  309:             while finalizers:
  185:                 fin = finalizers.pop()
  185:                 try:
  185:                     fin()
>>>>>>                 except TEST_OUTCOME as e:
>>>>>>                     these_exceptions.append(e)
       
  124:             if len(these_exceptions) == 1:
>>>>>>                 exceptions.extend(these_exceptions)
  124:             elif these_exceptions:
>>>>>>                 msg = f"errors while tearing down {node!r}"
>>>>>>                 exceptions.append(BaseExceptionGroup(msg, these_exceptions[::-1]))
       
   90:         if len(exceptions) == 1:
>>>>>>             raise exceptions[0]
   90:         elif exceptions:
>>>>>>             raise BaseExceptionGroup("errors during test teardown", exceptions[::-1])
   90:         if nextitem is None:
    2:             assert not self.stack
       
       
>>>>>> def collect_one_node(collector: Collector) -> CollectReport:
   36:     ihook = collector.ihook
   36:     ihook.pytest_collectstart(collector=collector)
   36:     rep: CollectReport = ihook.pytest_make_collect_report(collector=collector)
   36:     call = rep.__dict__.pop("call", None)
   36:     if call and check_interactive_exception(call, rep):
>>>>>>         ihook.pytest_exception_interact(node=collector, call=call, report=rep)
   36:     return rep
